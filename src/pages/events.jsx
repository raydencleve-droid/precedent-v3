import React from "react";
import Section from "../components/section.jsx";

export default function Events() {
    return (
        <Section title="Events">
            <p>Placeholder page. Add events and announcements here.</p>
        </Section>
    );
}
